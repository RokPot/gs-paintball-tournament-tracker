import FlexContainer from 'components/shared/FlexContainer';

interface IProps {}

const ScheduleView: React.FC<IProps> = () => {
  return <FlexContainer>Schedule View</FlexContainer>;
};

export default ScheduleView;
